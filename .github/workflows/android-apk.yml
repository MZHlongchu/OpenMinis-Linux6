name: Android APK

on:
  push:
    branches: [main]
    tags:
      - "v*"
      - "*-linux"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: android-apk-${{ github.ref }}
  cancel-in-progress: true

env:
  JAVA_VERSION: "17"
  ANDROID_PLATFORM: "android-36"
  ANDROID_BUILD_TOOLS: "35.0.0"
  ANDROID_CMAKE: "3.22.1"
  ANDROID_NDK: "28.0.13004108"
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=1g"

jobs:
  build:
    name: Build Minis Ultra APK
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: gradle

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y curl tar unzip make gawk sed file python3 cmake ninja-build g++ git

      - name: Install Android SDK commandline tools
        run: |
          mkdir -p /usr/local/lib/android/sdk/cmdline-tools
          cd /tmp
          curl -sL -o cmdline-tools.zip \
            "https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip"
          unzip -q cmdline-tools.zip -d /tmp/sdk
          mv /tmp/sdk/cmdline-tools /usr/local/lib/android/sdk/cmdline-tools/latest
          echo "ANDROID_SDK_ROOT=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"
          echo "ANDROID_HOME=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"
          yes | /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager --licenses >/dev/null || true

      - name: Install Android SDK components
        run: |
          /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager \
            "platform-tools" \
            "platforms;${ANDROID_PLATFORM}" \
            "build-tools;${ANDROID_BUILD_TOOLS}" \
            "cmake;${ANDROID_CMAKE}" \
            "ndk;${ANDROID_NDK}"
          echo "ANDROID_NDK_HOME=/usr/local/lib/android/sdk/ndk/${ANDROID_NDK}" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=/usr/local/lib/android/sdk/ndk/${ANDROID_NDK}" >> "$GITHUB_ENV"

      - name: Build PRoot
        run: |
          chmod +x deps/build_proot.sh
          ANDROID_NDK_HOME="/usr/local/lib/android/sdk/ndk/${ANDROID_NDK}" bash deps/build_proot.sh

      - name: Prepare Ubuntu sandbox + SDK assets
        run: |
          chmod +x scripts/prepare_android_sandbox.sh
          bash scripts/prepare_android_sandbox.sh
          test -f src/android/app/src/main/assets/ubuntu-base.tar.gz
          test -f src/android/app/src/main/assets/android-sdk-tools-aarch64.zip
          test -f src/android/app/src/main/assets/android-cmdline-tools.zip

      - name: Build rclone.aar
        run: |
          curl -sL -o /tmp/go.tar.gz https://go.dev/dl/go1.25.0.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf /tmp/go.tar.gz
          export PATH=/usr/local/go/bin:$PATH
          go version
          go install golang.org/x/mobile/cmd/gomobile@latest
          export PATH=$PATH:$(go env GOPATH)/bin
          gomobile init
          chmod +x deps/build_rclone_android.sh
          ANDROID_NDK_HOME="/usr/local/lib/android/sdk/ndk/${ANDROID_NDK}" bash deps/build_rclone_android.sh
          mkdir -p src/android/app/libs
          cp deps/build/rclone/rclone.aar src/android/app/libs/rclone.aar

      - name: Provider customization stub
        run: |
          test -f src/android/app/provider-customization.properties \
            || cp src/android/app/provider-customization.properties.example \
                  src/android/app/provider-customization.properties

      - name: Optional upload keystore
        env:
          B64: ${{ secrets.MINIS_UPLOAD_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$B64" ]; then
            echo "$B64" | base64 -d > /tmp/minis-upload.jks
            echo "MINIS_UPLOAD_STORE_FILE=/tmp/minis-upload.jks" >> "$GITHUB_ENV"
            echo "MINIS_UPLOAD_STORE_PASSWORD=${{ secrets.MINIS_UPLOAD_STORE_PASSWORD }}" >> "$GITHUB_ENV"
            echo "MINIS_UPLOAD_KEY_ALIAS=${{ secrets.MINIS_UPLOAD_KEY_ALIAS }}" >> "$GITHUB_ENV"
            echo "MINIS_UPLOAD_KEY_PASSWORD=${{ secrets.MINIS_UPLOAD_KEY_PASSWORD }}" >> "$GITHUB_ENV"
            echo "signed with repository upload keystore"
          else
            echo "No MINIS_UPLOAD_KEYSTORE_BASE64 — APK will be debug-signed (cannot replace a differently signed install)"
          fi

      - name: Cross-compile libunwind.a for aarch64
        env:
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/${{ env.ANDROID_NDK }}
          LLVM_SRC: ${{ github.workspace }}/.llvm-libunwind
          LIBUNWIND_BUILD: ${{ runner.temp }}/libunwind-build
        run: |
          chmod +x scripts/build_libunwind_aarch64.sh
          bash scripts/build_libunwind_aarch64.sh "$ANDROID_NDK_HOME"
          A=$(find "$ANDROID_NDK_HOME" -path '*sysroot/usr/lib/aarch64-linux-android*' -name libunwind.a | head -n 1)
          test -n "$A" && test -f "$A"
          echo "aarch64 libunwind.a: $A"

      - name: Assemble release APK
        working-directory: src/android
        env:
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/${{ env.ANDROID_NDK }}
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
        run: |
          sed -i 's|org.gradle.jvmargs=.*|org.gradle.jvmargs=-Xmx6g -Dfile.encoding=UTF-8|' gradle.properties
          chmod +x gradlew
          ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Stage APK
        run: |
          SRC=src/android/app/build/outputs/apk/release/app-release.apk
          test -s "$SRC"
          mkdir -p dist
          cp "$SRC" dist/minis-ultra-com.openminis.linux.apk
          unzip -l "$SRC" | grep -E "libproot|proot-aarch64|ubuntu-base|android-sdk-tools|android-cmdline-tools" || true
          NDK="/usr/local/lib/android/sdk/ndk/${ANDROID_NDK}"
          UNWIND_SO=$(find "$NDK" -path '*aarch64-linux-android*' -name libunwind.so 2>/dev/null | head -n 1 || true)
          UNWIND_A=$(find "$NDK" -path '*aarch64-linux-android*' -name libunwind.a 2>/dev/null | head -n 1 || true)
          if [ -n "$UNWIND_SO" ]; then
            cp "$UNWIND_SO" dist/libunwind.so
            echo "staged libunwind.so from $UNWIND_SO"
          fi
          if [ -n "$UNWIND_A" ]; then
            cp "$UNWIND_A" dist/libunwind.a
            echo "staged libunwind.a from $UNWIND_A"
          fi
          if [ -z "$UNWIND_SO" ] && [ -z "$UNWIND_A" ]; then
            echo "libunwind not in NDK sysroot — see docs/SIGNING.md"
          fi
          ls -lh dist/

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: minis-ultra-apk
          path: dist/minis-ultra-com.openminis.linux.apk
          if-no-files-found: error

      - name: Read app version
        id: appver
        run: |
          CODE=$(sed -n 's/.*versionCode = \([0-9][0-9]*\).*/\1/p' src/android/app/build.gradle.kts | head -1)
          NAME=$(sed -n 's/.*versionName = "\(.*\)".*/\1/p' src/android/app/build.gradle.kts | head -1)
          echo "code=$CODE" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "app versionName=$NAME versionCode=$CODE"

      - name: Publish rolling pre-release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: android-latest
          name: Minis Ultra (Android)
          prerelease: true
          generate_release_notes: false
          files: dist/*
          body: |
            Rolling Android build from `${{ github.sha }}`.

            - applicationId: `com.openminis.linux`
            - launcher name: **Minis Ultra**
            - arm64-v8a (debug-signed unless MINIS_UPLOAD_KEYSTORE_BASE64 is set)
            - versionName: `${{ steps.appver.outputs.name }}`
            - versionCode: ${{ steps.appver.outputs.code }}
            - signing: see docs/SIGNING.md

      - name: Publish versioned release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false
          prerelease: false
          files: dist/*
          body_path: docs/RELEASE-NOTES.zh.md
